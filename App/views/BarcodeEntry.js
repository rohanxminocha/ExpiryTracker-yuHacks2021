import React, {Component} from 'react';
import {View, Text, SafeAreaView} from 'react-native';

const BarcodeEntry = ({navigation, route}) => {
    return(
        <SafeAreaView>
            <Text>BarcodeEntry</Text>
        </SafeAreaView>
    );
}


export default BarcodeEntry